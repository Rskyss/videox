# 验收记录 · v0.7 全平台统一下载进度

一句话：抖音、B站、小红书、YouTube、TikTok、Twitter/X 全部使用统一后台任务、进度条和临时文件清理流程。

## 本版范围

- YouTube 支持 360p、720p、1080p，自动下载并合并音视频
- 其他支持平台按源站最佳可用格式下载
- DASH/HLS 使用 yt-dlp 实时进度；普通媒体直链按字节报告进度
- 无法获知总大小时显示动态进度条，不再只有按钮转圈
- 完成文件传输后立即删除；未领取文件一小时自动过期
- 修复 HTML 网关错误被前端当作 JSON 解析的问题
- 补充 `yt-dlp-ejs` 与兼容范围内的 `curl-cffi` 依赖
- TikTok 缺少浏览器模拟组件时不再向页面暴露 Python traceback

## 自测证据

- 自动化：`python -m unittest -v test_download_jobs.py test_frontend_download.py test_seo.py`，24 项全部通过
- 语法：`python -m py_compile app.py downloader.py` 与 `node --check static/script.js` 通过
- 差异：`git diff --check` 通过
- YouTube：真实 1080p 短视频下载并合并成功，H.264 1920×1080 + AAC
- B站：真实 23:21 视频进度为 15% → 37% → 84% → 100%，视频与音频轨道完整
- TikTok：真实解析返回 200，下载进度为 24% → 100%，HEVC 720×1280 + AAC

## 已声明边界

- `cookies.txt` 不纳入提交
- 进行中的 `docs/自然流量增长/` 不纳入提交
- 本次发布目标为 GitHub；生产服务器部署需另行执行

## 用户验收

2026-08-07 用户确认本地测试完毕，并要求按推送规则发布到 GitHub，视为 v0.7 发布指令。
